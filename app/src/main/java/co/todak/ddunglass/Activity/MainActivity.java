package co.todak.ddunglass.Activity;import android.Manifest;import android.app.Activity;import android.content.Intent;import android.content.pm.PackageManager;import android.os.Build;import android.provider.MediaStore;import android.support.v4.app.ActivityCompat;import android.support.v7.app.AppCompatActivity;import android.os.Bundle;import android.view.View;import android.widget.Button;import android.widget.ImageView;import co.todak.ddunglass.R;public class MainActivity extends AppCompatActivity {    private final int REQUEST_CODE = 100;    String mPath;    Button btn_next;    ImageView iv_titleScreen;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_main);        // 권한 체크        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {            // 카메라            int permission_camera = ActivityCompat.checkSelfPermission(this, Manifest.permission.CAMERA);            // 저장소 읽기            int permission_readStorage = ActivityCompat.checkSelfPermission(this, Manifest.permission.READ_EXTERNAL_STORAGE);            // 저장소 쓰기            int permission_writeStorage = ActivityCompat.checkSelfPermission(this, Manifest.permission.WRITE_EXTERNAL_STORAGE);            // 따로 따로 권한 요청이 되지 않아            // 조건문을 사용하여 하나라도 없으면 권한 요청            if ((ActivityCompat.checkSelfPermission(this, Manifest.permission.CAMERA) != PackageManager.PERMISSION_GRANTED) || (ActivityCompat.checkSelfPermission(this, Manifest.permission.READ_EXTERNAL_STORAGE) != PackageManager.PERMISSION_GRANTED) || (ActivityCompat.checkSelfPermission(this, Manifest.permission.WRITE_EXTERNAL_STORAGE) != PackageManager.PERMISSION_GRANTED)) {                requestPermissions(new String[]{Manifest.permission.CAMERA, Manifest.permission.READ_EXTERNAL_STORAGE, Manifest.permission.WRITE_EXTERNAL_STORAGE}, REQUEST_CODE);            }        }        // xml 에서 지어 준 id 와 버튼 객체를 연결시켜 아래서        // 클릭 리스너를 통해 이벤트를 만들어 줌        btn_next = (Button) findViewById(R.id.btn_next);        iv_titleScreen = (ImageView) findViewById(R.id.iv_titleScreen);        // DDunglassActvity 로 화면을 넘기고 현재 액티비티를 닫음        btn_next.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View v) {                Intent intent = new Intent(getApplicationContext(), DDunglassActvity.class);                startActivity(intent);                finish();            }        });    }}